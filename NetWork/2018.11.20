# 一.分组交换：
  - 分组交换采用存储转发技术，发送的整块数据成为一个报文，在发送报文前，把较长的报文划分为更小的等长数据段，每个数据段为1024bit，在每个数据段前加入首部（header）
  就是一个分组，分组也称为包，分组的首部称为包头，分组是因特网中传输的数据单元，首部包含了目的地址和原地址等信息。
  
  - 路由器是用来转发分组的，即分组交换。路由器收到了分组，先检查头部信息，然后查找转发表进行查找，最后转发到目的地址。
